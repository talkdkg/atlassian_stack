1) PARAMS Check

	verify JVM_MAXIMUM_MEMORY is ok, put it large enough

2) STARTUP/STOP

	Use:

	docker container stop jira
	docker container start jira

	Remembrr: If you recreate containers you need to re-import certificates!.