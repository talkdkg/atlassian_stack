docker-compose up -d
docker-compose logs

Open pg_admin: http://${CONFLUENCE_IP}:9999/browser/ & user pgadmin
Create new connection: postgres: changeit

postgres: ${CONFLUENCE_IP}:64320

In postgres:
	1) create new user: Login/Group Roles -> Name:${ADMIN_USER}, password: ${ADMIN_PASS}, privileges: Can login? true, Save
	2) create db: Create-> Database -> jira/confluence/crowd, owner ${ADMIN_USER}, collation utf-8